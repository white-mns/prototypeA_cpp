#pragma once
#include <stdio.h>
#include <string>

namespace BattleSystem{

    class Battle{
        public:
            Battle(void);
            ~Battle(void);

            void Execute();
    };
}
